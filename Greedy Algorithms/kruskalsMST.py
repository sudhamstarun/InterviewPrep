# Picking smallest weight edge that does not form a cycle


def kruskalsMST(startTime, FinishTime):
    n = len(FinishTime)

    iterator = 0
    print("The Value of iterator is: ", iterator)

    for another_iterator in xrange(n):
